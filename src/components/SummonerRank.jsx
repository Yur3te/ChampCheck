import React, {useState} from 'react'

function SummonerRank(props) {

    //todo trza naprawić robienie wrappera

    // const [rank, setRank] = useState("")
    // props.wrapper.leagueTest(props.id).then((response) => {
    //     setRank(response.data)
    // })

    return (
        <div>
            
        </div>
    )
}

export default SummonerRank
